// Address of the game's server (Vercel): run tickets, score checks, the leaderboard,
// handle verification and share cards. Empty = the game runs offline, without them.
window.RED_CANDLE_CONFIG = {
  server: 'https://red-candle-lime.vercel.app',
};
